import React from 'react';


interface Props {
  data: ExtensionStorage,
  onchange: (value: ExtensionStorage) => void,
}

export const AdvanceBlock: React.FC<Props> = ({ data, onchange }) => {
  return (
    <div></div>
  );
};
